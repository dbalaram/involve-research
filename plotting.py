import matplotlib.pyplot as plt

def plot_data(data):
    """
    Plot relevant data.
    
    Parameters:
    data (pandas.DataFrame): DataFrame containing relevant data.
    """
    # Example plot (to be replaced by user-defined plot logic)
    data.plot(kind='bar')
    plt.show()
